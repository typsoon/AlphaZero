---
name: enforce-testability
description: Enforces testable code design and requires generating useful tests for all new code, and adding them to the test workflows.
---

# Enforce Testability

The code written by you should be testable if possible (so design it carefully). Generate as much useful tests as possible when adding code and add them to the CI workflows and dodo.py tasks.
